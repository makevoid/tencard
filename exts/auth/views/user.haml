

#my_user


  %h1= @user.username

  - if me?
    %nav.inpage
      - link_to "Edit profile", "/users/#{@user.id}/edit", class: "btn"

  .card_info
    Card: #{@user.card_code}

  %div


    - if File.exist? "#{TenCard.root}/views/_ext_user.haml"
      = partial :ext_user

    .clear